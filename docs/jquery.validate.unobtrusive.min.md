## jQuery Validate Unobtrusive

The jQuery Validate Unobtrusive plugin provides a way to validate form data without the need for explicit JavaScript validation.

### Usage
javascript
$.validator({
  // options
});


### Methods
#### `validate()`: Validates the form data
#### `resetForm()`: Resets the form data
#### `addMethod(methodName, callback)`: Adds a custom validation method
